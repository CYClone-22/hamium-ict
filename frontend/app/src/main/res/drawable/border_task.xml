<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/transparent"/> <!-- 배경은 투명하게 설정 -->
    <stroke
        android:width="2dp"
        android:color="@color/colorLightPurple"/> <!-- 테두리 색상 및 두께 설정 -->
    <padding
        android:left="7dp"
        android:right="7dp"
        android:top="7dp"
        android:bottom="7dp"/> <!-- 패딩을 설정하여 테두리가 내용과 겹치지 않게 함 -->
</shape>
